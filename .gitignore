# OmniNode reference checkouts (local only — do not push)
omniclaude/
omnimarket/
omnimarket-main/
omnimarket-main.zip
omnidash/
omnidash-main/

# Environment files (credentials, local overrides)
.env
.env.local
.env.*.local

# macOS
.DS_Store
*.pages

# IDE / editor
.vscode/
.claude/settings.local.json
# Claude Code worktrees — a nested repo committed as a phantom gitlink (no
# .gitmodules entry) breaks actions/checkout's submodule cleanup on every job.
.claude/worktrees/

# Python
__pycache__/
*.pyc
.venv/
dist/
build/
*.egg-info/

# Tools
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OmniCursor local data
.omnicursor/
.worktrees/

# Personal notes / research (not part of the project)
investigacion_omninode_ClaudeCode.md
OmniClaude summary.md
omnicursor-team-guidance.md