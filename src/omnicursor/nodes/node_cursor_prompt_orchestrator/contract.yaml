# SPDX-FileCopyrightText: 2025 OmniNode.ai Inc.
# SPDX-License-Identifier: MIT
# OmniCursor node contract — Cursor-native analogue of OmniClaude skill orchestrators.
# Implementation: .cursor/hooks/scripts/user-prompt-submit.py (stdlib only; must not import omnicursor).

name: node_cursor_prompt_orchestrator
contract_name: node_cursor_prompt_orchestrator
node_name: node_cursor_prompt_orchestrator

contract_version:
  major: 1
  minor: 0
  patch: 0

node_version:
  major: 1
  minor: 0
  patch: 0

node_type: ORCHESTRATOR_GENERIC

description: >
  Classifies the user prompt against agent configs and emits the classification
  (matched agent, score, relevant pattern ids, delegation signal) for backend
  learning. Block/observe-only: Cursor's beforeSubmitPrompt output is
  {continue, user_message} and cannot inject context — this hook returns
  {"continue": true}. Context injection lives in sessionStart / postToolUse
  (node_cursor_pattern_injection_compute / node_cursor_tool_use_compute).

cursor_native:
  hook_event: beforeSubmitPrompt
  hooks_json_command: "python3 .cursor/hooks/scripts/user-prompt-submit.py"
  implementation: .cursor/hooks/scripts/user-prompt-submit.py
  blocking: false

execution:
  backend: cursor_ide
  notes: Deterministic hook; no LLM inside the hook process.

local_event_bus:
  kind: append_only_jsonl
  path: "~/.omnicursor/events.jsonl"
  description: >
    Optional telemetry from _common.log_event; not a Kafka topic.

capabilities:
  - name: cursor.prompt_classification
    description: Merge-layer agent scoring + hook-event emission (no injection)
    version: 1.0.0

metadata:
  license: MIT
  parity_reference: omniclaude shared routing / UserPromptSubmit hooks
  tags:
    - orchestrator
    - cursor
    - beforeSubmitPrompt
